{
  "name": "trafficmap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TrafficMap passive full-page network traffic dashboard.",
  "scripts": {
    "start": "node src/agent.js",
    "check": "node scripts/check-syntax.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage",
    "verify:repo": "node scripts/verify-repository.js",
    "verify": "npm run check && npm test && npm run verify:repo"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
